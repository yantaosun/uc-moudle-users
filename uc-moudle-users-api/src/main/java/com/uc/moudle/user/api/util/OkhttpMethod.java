package com.uc.moudle.user.api.util;import okhttp3.FormBody;import okhttp3.OkHttpClient;import okhttp3.Request;import okhttp3.Response;import java.io.IOException;import java.util.HashMap;import java.util.Set;/** * @author 9527 * @ClassName OkhttpMethod * @Date 2020/1/21 * @Description TODO * @Version 1.0 */public class OkhttpMethod {    /**     * 发送post请求     * @param url     * @param paramsMap     * @return     */    public static Response sendPost(String url, HashMap<String, String > paramsMap) throws IOException {  //这里没有返回，也可以返回string        OkHttpClient mOkHttpClient = new OkHttpClient();        FormBody.Builder formBodyBuilder = new FormBody.Builder();        Set<String> keySet = paramsMap.keySet();        for(String key:keySet) {            String value = paramsMap.get(key);            formBodyBuilder.add(key,value);        }        FormBody formBody = formBodyBuilder.build();        Request request = new Request                .Builder()                .post(formBody)                .url(url)                .build();        Response response = mOkHttpClient.newCall(request).execute();        return response;    }    /**     * 发送Get请求     * @param url     * @return     * @throws Exception     */    public static Response sendGet(String url) throws Exception{        OkHttpClient okHttpClient = new OkHttpClient();        Request request = new Request                .Builder()                .url(url)                .get()                .build();        Response response = okHttpClient.newCall(request).execute();        return  response;    }}